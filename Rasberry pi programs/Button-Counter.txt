#import the tinker module
import Tkinter
#create a new window
window = Tkinter.Tk()
#set window name
window.title("Commands")
#set window size
window.geometry("300x300")
#Decareren variable
presses =0
#add fuction for times clicked
def addClick():
        #Globel is used that we can use the variable oustide the function
        global presses
        presses +=1
        lbl.configure(text=presses)

#a function that prints a message on the screen
def callback():
        print("Button clicked")
        lbl.configure(text="Button Clicked")

#Create a lable

lbl = Tkinter.Label(text="Nothing to do here")
lbl.pack()

#Create a new button and provide argumlent command
#witch calles the fuction callback

btn = Tkinter.Button(window, text="click me", command= addClick)
btn.pack()



#set background
window.configure(background="#336666")

#draw the window, and start the app
window.mainloop()
